<h1><%= @patient.name %></h1>

<h3>Appointments:</h3>
<ul>
  <% @patient.appointments.each do |appt| %>
    <li>
      <%= appt.appointment_datetime.strftime("%B %-d, %Y at %k:%M") %> with
      <%= link_to appt.doctor_name, doctor_path(appt.doctor) %>
    </li>
  <% end %>
</ul>