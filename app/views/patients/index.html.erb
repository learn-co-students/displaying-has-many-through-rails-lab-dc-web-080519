<h1>Patients Directory</h1>

<ul>
<% @patients.all.each do |patient| %>
  <li><%= link_to "Name: #{patient.name} Number of Appointments: #{patient.appointments.count}", patient_path(patient)%>
  </li>
<% end %>
</ul>
