<h1><%= @doctor.name %></h1>

<ul>
  <% @doctor.appointments.each do |appt| %>
    <li>
      <%= appt.appointment_datetime.strftime("%B %-d, %Y at %k:%M") %>:
      <%= link_to appt.patient_name, patient_path(appt.patient) %>
  </li>
  <% end %>
</ul>
